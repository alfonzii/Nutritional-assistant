<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nutritionalassistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DatabaseActivity"></activity>
        <activity android:name=".GoalActivity" />
        <activity
            android:name=".CurrentLifestyleActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UserParametersActivity" />
        <activity
            android:name=".NHSetActivity"
            android:label="Nutrition Settings"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".FoodAddingActivity"
            android:label="Food Adding Activity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ManualNHActivity"
            android:label="Manual Nutrition Settings"
            android:parentActivityName=".NHSetActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>